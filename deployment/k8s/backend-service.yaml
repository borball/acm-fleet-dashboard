apiVersion: v1
kind: Service
metadata:
  name: acm-fleet-backend
  namespace: acm-fleet
  labels:
    app: acm-fleet
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: acm-fleet
    component: backend

