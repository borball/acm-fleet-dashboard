apiVersion: v1
kind: Service
metadata:
  name: acm-fleet-frontend
  namespace: acm-fleet
  labels:
    app: acm-fleet
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: acm-fleet
    component: frontend

