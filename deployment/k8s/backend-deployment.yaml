apiVersion: apps/v1
kind: Deployment
metadata:
  name: acm-fleet-backend
  namespace: acm-fleet
  labels:
    app: acm-fleet
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: acm-fleet
      component: backend
  template:
    metadata:
      labels:
        app: acm-fleet
        component: backend
    spec:
      serviceAccountName: acm-fleet
      containers:
      - name: backend
        image: quay.io/bzhai/acm-fleet-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8080"
        - name: ENABLE_AUTH
          value: "true"
        - name: OAUTH_ISSUER_URL
          value: "https://kubernetes.default.svc"
        - name: LOG_LEVEL
          value: "info"
        resources:
          limits:
            cpu: "1"
            memory: 1Gi
          requests:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/live
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          runAsNonRoot: true

